# Local (Vite dev)
VITE_API_URL=http://127.0.0.1:3333

# Cloudflare Worker (runtime) — use este nome no dashboard do CRM:
# API_URL=https://api.agenciatoro.com.br
#
# Tambem aceito no build (opcional):
# VITE_API_URL=https://api.agenciatoro.com.br
